import ReactDOM from 'react-dom';

import './index.css';
import App from './App';

// First file that will execute at first


// The below code rendeers app instead of the root element find in the indexedDB.html file in [ublic]
ReactDOM.render(<App />, document.getElementById('root'));

